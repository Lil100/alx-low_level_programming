#include "main.h"
#include <stdio.h>
/**
 *  _abs - function taht computes the absolute value of an integer
 *  @c: is the int that will use for the agument
 *  Return :0
 */
int _abs(int c)
{
	if (c > 0 || c == 0)
	{
		returno (c);
	}
	else
		return (c * -1);
}
